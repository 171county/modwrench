# ─── Legacy API keys (developer testing path) ────────────────────────────────
# Used when no OAuth token is in the OS keychain. Production users should
# prefer `modwrench-<server> auth login` instead — see VISION.md.

# Nexus Mods Personal API Key
# Get yours: https://www.nexusmods.com/users/myaccount?tab=api+access
NEXUS_API_KEY=your_nexus_personal_api_key_here

# mod.io read-only API Key (also used to bootstrap the email-exchange OAuth flow)
# Get yours: https://mod.io/me/access
MODIO_API_KEY=your_modio_api_key_here

# ─── OAuth (optional) ────────────────────────────────────────────────────────
# Register a Nexus OAuth app under your account's "API access" tab and paste
# the client_id here. Required only for `modwrench-nexus auth login`.
# NEXUS_OAUTH_CLIENT_ID=
# NEXUS_OAUTH_CLIENT_SECRET=   # only if your Nexus OAuth app is a confidential client

# ─── Optional ────────────────────────────────────────────────────────────────
# Log level for ModWrench servers
# Values: debug | info | warn | error
LOG_LEVEL=info